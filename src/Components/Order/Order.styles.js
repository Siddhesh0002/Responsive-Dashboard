import { createStyles } from "@mantine/styles";

export default createStyles(() => ({
  orderPaper: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    borderRadius: 20,
  },
  itemHead: {
    color: "#5b5c61",
    fontWeight: 700,
    fontSize: 25,
  },
}));
